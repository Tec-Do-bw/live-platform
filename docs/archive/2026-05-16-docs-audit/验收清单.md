# 文档整顿验收清单

## Commit 14297e0 - docs: 按四类目录约定整顿文档结构

- [a] ✅ 顶层无散文件：`docs/*.md` 返回空（no matches），`find services/*/docs -maxdepth 1 -name "*.md"` 返回空。所有文档均位于 specs/designs/plans/archive 四类子目录中。
- [b] ✅ 旧路径引用清零：对全部 7 个被移动文件的旧路径执行 grep（排除 archive/2026-05-16-docs-audit/），均返回空集。无残留引用。
- [c] N/A — 本 commit 无 paths 改动。
- [d] ✅ 单一权威源抽样通过：
  - 「限界上下文」详细定义仅在 `docs/designs/DDD-domain-model-design.md:29`，其余为概述性引用
  - 「主备高可用」机制详述仅在 `services/live-monitor/CLAUDE.md:9`，其余为概述性提及
  - 「GMV 采集频率」具体参数仅在 `docs/specs/PRD-live-platform-v1.0.md:511`，其余为功能列举
- [e] ✅ CLAUDE.md 行数均在 200 行以内：根 113 行，live-platform 28 行，live-crawler 58 行，adspower-server 34 行，live-stream 49 行，live-monitor 37 行。
- [f] ✅ 分支确认：`git rev-parse --abbrev-ref HEAD` 输出 `codex/live-platform-phase1`，未偏离。

**结论**：通过

## Commit c2fca01 - chore: 为 chinese-comments 规则添加 shell 脚本路径匹配

- [a] N/A — 本 commit 未移动文件。
- [b] N/A — 无旧路径需验证。
- [c] ✅ paths glob 验证通过：
  - `git ls-files '**/*.sh'` → `services/live-stream/start.sh`（匹配成功，无误伤）
  - 原有 glob 未被破坏：`**/*.py` 匹配 5+ 文件，`**/*.vue` 匹配 5+ 文件，`**/*.js` 匹配 5+ 文件，`**/*.ts` 无文件但 glob 语法有效
- [d] N/A — 本 commit 无文档内容变更。
- [e] ✅ CLAUDE.md 行数未变：根 113 行，live-platform 28 行，live-crawler 58 行，adspower-server 34 行，live-stream 49 行，live-monitor 37 行。
- [f] ✅ 分支确认：`git rev-parse --abbrev-ref HEAD` 输出 `codex/live-platform-phase1`，未偏离。

**结论**：通过

## Commit 16907dc - docs: 新增上下文按需加载指引，根 CLAUDE.md 添加引用

- [a] N/A — 本 commit 未移动文件。
- [b] N/A — 无旧路径需验证。
- [c] ✅ 新文件 `.claude/docs/context-loading-guide.md` 存在（29 行），内容为任务场景→文档映射表，与根 CLAUDE.md 工作流规则表格视角不同（前者面向任务执行者，后者面向规则维护者），不构成重复。根 CLAUDE.md 仅新增一行引用。
- [d] ✅ 单一权威源验证：新文件第 26-29 行明确声明与工作流规则表格的关系区分，两者描述维度不同（场景映射 vs 规则触发条件），不违反单一权威源。
- [e] ✅ 根 CLAUDE.md 行数 114 行（+1 行引用），仍在 200 行以内。其余 CLAUDE.md 未变。
- [f] ✅ 分支确认：`git rev-parse --abbrev-ref HEAD` 输出 `codex/live-platform-phase1`，未偏离。

**结论**：通过

---

## Executive Brief

### a. 文件移动与引用改动统计

- **文件移动总数**：7 个（Commit 1: 14297e0）
  - docs/ 顶层 → designs/: 1 个（DDD-domain-model-design.md）
  - docs/ 顶层 → specs/: 1 个（PRD-live-platform-v1.0.md）
  - docs/ 顶层 → archive/: 1 个（architecture-review-2026-05-03.md）
  - docs/guides/ → archive/: 1 个（agent-team-guide.html）
  - services/live-crawler/docs/superpowers/ → docs/archive/: 3 个
- **引用改动数**：0（grep 验证所有旧路径在非 archive 区域无残留引用，说明这些文件此前未被其他文档引用）
- **paths 改动**：1 处（Commit 2: chinese-comments.md 新增 `**/*.sh`）
- **新增文件**：1 个（Commit 3: `.claude/docs/context-loading-guide.md`，29 行）

### b. paths 改造前后「全局加载规则数」对比

| 指标 | 改造前 | 改造后 |
|------|--------|--------|
| 带 paths 的 rules 文件数 | 4 | 4（不变） |
| chinese-comments.md glob 条目数 | 4（py/ts/vue/js） | 5（+sh） |
| 总 glob 规则条目数 | 全部 rules 文件合计 | +1 条（仅新增 `**/*.sh`） |

改造幅度极小，仅为 chinese-comments 规则补充了 shell 脚本覆盖。

### c. 三个未被采纳的 Devil's Advocate 反对意见及保留理由

| # | 反对意见 | 未采纳理由 |
|---|---------|-----------|
| 1 | **#1 七文件移动 ROI 不足，建议缩减为 3 个** | 未采纳。4 个过期 superpowers/guides 文件虽无人引用，但留在原位违反四类目录约定，且归档成本极低（纯 git mv，无引用需更新）。统一归档消除了后续维护者的认知负担。 |
| 2 | **#2 Dockerfile glob 过度触发** | 采纳（未实施）。本次迁移未新增 Dockerfile/docker-compose glob，避免了触发后无有效行动的问题。 |
| 3 | **#4 Superpowers 一刀切禁止服务级目录** | 部分采纳。实际执行时将 3 个过期 superpowers 文件归档到 `docs/archive/`，理由是「过期归档」而非「位置违规」，保留了未来服务级 superpowers 目录的可能性。 |

### d. 方法论摘要条款采纳情况

**已采纳条款**：
- **条款 a（paths 粒度）**：新增 `**/*.sh` 遵循「具体扩展名优先」原则
- **条款 c（行数上限）**：所有 CLAUDE.md 均控制在 200 行以内（最大 114 行）
- **条款 d（monorepo 分工）**：根文件仅加一行引用，详细内容放 `.claude/docs/`，遵循「禁止复制副本，用路径引用」

**未采纳条款及原因**：
- **条款 b（@import 时机）**：本次无 @import 操作，不适用
- **条款 e（程序化校验）**：本次为文档整顿，未涉及 CI/hook 改造，留待后续迭代

### e. 后续建议（遗留项）

1. **Dockerfile/docker-compose 规则**：如未来服务新增 Dockerfile，应创建独立规则 `read-deployment-docs-before-edit.md`（指向部署文档而非 CLAUDE.md）
2. **根 CLAUDE.md 瘦身监控**：当前 114 行，距 150 行警告线尚有余量，但应在每次新增内容时评估是否可拆分到 `.claude/docs/` 或 `.claude/rules/`
3. **services/live-crawler/docs/superpowers/ 目录清理**：原目录已空（3 文件已归档），可在下次迭代中删除空目录
4. **`**/*.ts` glob 实效性**：当前项目无 .ts 文件，chinese-comments 规则中该条目为预留，待前端 TypeScript 化后生效
5. **反对意见 #3（上下文加载指引与工作流规则表格的边界）**：已通过文件内声明（第 26-29 行）解决，但应在后续维护中持续关注两者是否出现内容漂移

---

**校验完成时间**：2026-05-20
**校验人**：Verifier（汇聚校验 Agent）
**总结**：3 个 commit 全部通过，文档整顿工程按四类目录约定完成，无残留引用，paths 改造最小化，CLAUDE.md 行数可控。
