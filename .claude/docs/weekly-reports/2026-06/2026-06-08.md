---
week: 2026-06-01 ~ 2026-06-05
created: 2026-06-08
---

# 周报 2026-06-01 ~ 2026-06-05

## 【填入:本周工作】输入框

> 主线:TikTok HTTP 采集链路深化 + 采集底座稳定性治理。6 个提交 / 49 文件 / +3262 -698 行。

**📊 一、本周产出 Highlight**

- **TikTok HTTP 三链路体系化(adapter / collector / refresher)**
  - 新增 `tiktok-http-lifecycle.md` 规则总览,修正 4 条已有规则覆盖 HTTP 链路
  - 新增 `tiktok_refresh_routes` 刷新接口(X-API-Token 鉴权 + 线程隔离)
  - 养号刷新改 `curl_cffi` 直连 `account_info` 验证登录态,登出回调统一基于 HTTP 结果
  - 登录成功自动触发凭据刷新,带 profile 冲突防护

- **TikTok HTTP 全量时间窗配置化 + 当地 13 点补采**
  - 新增 `TIKTOK_HTTP_CONFIG`,全量回溯默认 60 天(可固定起始日期),不再硬写 28 天
  - `live/list` 启用翻页(单页 500、最多 10 页),6 时区(~300 账号)新增当地 13 点 TikTok-only 补采任务
  - `LIVE_LIST_STATS_TYPES` 字段集与页面下发对齐,更新采集账号列表
  - OpenAI 客户端迁移 DeepSeek API(deepseek-v4-flash)

- **跨平台 dataSource 字段统一**
  - 新增 `crawlers/constants.py` 定义 `live_crawler_{tiktok,shopee,lazada}`
  - 基类增加抽象方法 `get_data_source()`,5 个 format 方法上报注入 `dataSource` 供下游过滤

- **日志隔离与多源聚合**:logger 按启动入口隔离,`log_parser` 支持合并多源解析,cron 退役

- **🐛 Downloader P0 故障修复**:并发共享实例时大面积超时,双根因(重试就地改写共享 client 引发线程踩踏 + 静态 IP 终身复用单点)。改为每次尝试动态选代理 + 局部 client,一次性解决线程安全与 IP 轮换

**✅ 二、核心交付**
- TikTok HTTP 三链路规则单一权威源 + 刷新接口上线 + 登出回调铁律落地
- 全量窗口配置化、`live/list` 翻页、6 时区 13 点补采任务
- 跨 3 平台 dataSource 字段统一,基类强制新平台实现
- Downloader 并发稳定性双根因修复
- 进度正常,无延期风险

---

## 【填入:下周计划】输入框

- **P0**:① 观察 TK HTTP 300+ 账号采集情况(成功率 / 翻页触顶 / 各时区补采落点),沉淀监控阈值;② 重构优化 Shopee 多国跨境本土登录验证;③ 接手对接 TT Live 素材 / 视频采集(对齐契约与接入点,产出实施计划)
- **P1**:TikTok HTTP P2 待办清理;daily_report 多源聚合完善
- **P2**:预留 TK 300 账号运行期突发问题 + Shopee 重构联调多国权限差异
